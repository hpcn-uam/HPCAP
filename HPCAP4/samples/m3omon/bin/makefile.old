//CFLAGS = -g -pg -lpcap -lm -lpthread -Wall -D_GNU_SOURCE

CFLAGS = -g -lpcap -lm -lpthread  -lhpcap -Wall -D_GNU_SOURCE -L../../../lib

CC = gcc

all: monitor_flujos

monitor_flujos: monitor.c aux.c IPflow.c list.c monitor.h aux.h IPflow.h list.h
	@ echo
	@ echo Creando ejecutable  monitor
	$(CC) -o monitor_flujos monitor.c aux.c IPflow.c list.c $(CFLAGS)
clean: 
	rm -fr core *.o monitor_flujos
